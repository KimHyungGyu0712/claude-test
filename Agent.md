# Agent.md — 독학재수학원 관리 앱 프로젝트

이 파일은 AI 에이전트(Claude Code 등)와 개발자 본인이 프로젝트의 현재 상황과 방향을 공유하기 위한 문서입니다.
작업을 시작하기 전에 항상 이 파일을 먼저 읽습니다.

---

## 1. 프로젝트 개요

- **프로젝트명:** 독학재수학원 관리 앱 (가칭)
- **목적:** 독학재수학원의 학생 생활 관리와 선생님의 행정 업무를 하나의 시스템으로 통합
- **기획서:** https://docs.google.com/document/d/1toJZZtUHiBB1xTHc8bCVtW7KmB3ZqnikHmA4D6cLJZ4/edit
- **프로젝트 폴더:** `C:\Users\khgg0\orca\claude-test` (Git 저장소)
- **작성일:** 2026-09-05

## 2. 현재 상황

- 개발자는 **개발 경험이 전혀 없는 초보자**입니다. 프로그래밍, 앱 개발, 서버, 데이터베이스 모두 처음입니다.
- 기획서(구글 문서)는 작성되어 있고, 선생님용 데스크톱 UI와 학생용 모바일 UI 시안 링크가 포함되어 있습니다.
- `app/` 에 Flutter 프로젝트(hagwon_app)가 있고, 선생님용 웹 화면(로그인, 대시보드, 학생 목록, 벌점, 도시락, 질문, 민원, 공지, 계정 관리)이 **가짜 데이터(mock)** 로 동작하는 상태입니다. Firebase는 아직 연결하지 않았습니다.
- 기술 스택은 **Flutter + Firebase로 확정**되었습니다 (2026-09-05, 상세는 7번과 `개발기획서.md` 2번).
- 개발 환경 세팅 **완료** (2026-09-05, 상세는 7-1번). `flutter analyze`, `flutter test` 통과.
- 이전 작업 폴더 `C:\Users\khgg0\OneDrive\Desktop\학원어플` 은 폴더명이 한글이라 Android 빌드가 실패해 폐기했습니다. 여기(영문 경로)에서만 작업합니다.

## 3. 대상 플랫폼

| 사용자 | 플랫폼 | 비고 |
|--------|--------|------|
| 선생님 | 웹사이트 (PC 브라우저) | 설치 불필요. 같은 Flutter 코드로 웹 빌드. 태블릿/폰 브라우저에서 QR 스캔 순찰에도 활용 |
| 학생 | Android 앱 (우선), iOS 앱 (후순위) | 자리에서 모든 요청을 처리. iOS는 Mac 확보 후 빌드 |

## 4. 핵심 기능 (기획서 기준)

### 학생 앱 — "자리에서 일어나지 않고 모든 것을 해결"
- 도시락 신청
- 질문 접수 (선생님에게 질문 예약)
- 민원 신고
- 공지사항 확인
- 벌점 조회

### 선생님 앱 — "현장 순찰의 효율성과 즉각적인 행정 처리"
- QR 스캔 순찰 (학생 자리/출석 확인)
- 벌점 부여
- 도시락 관리
- 질문 예약 배정
- 민원 처리
- 알림 발송

### 공통 고려사항
- 미성년자 개인정보 보호 (수집 최소화, 보호자 동의, 안전한 저장)
- 다중 플랫폼 지원

## 5. 개발 기간: 3개월 (2026-09-08 ~ 2026-12-05)

초보자 기준으로 현실적인 일정입니다. 기능을 한 번에 다 만들지 않고, **가장 중요한 기능부터 작동하는 상태로** 만들어 나갑니다.

| 기간 | 목표 |
|------|------|
| 1~2주차 | 개발 환경 세팅, 기술 스택 결정, 기초 학습 (언어 문법, 앱 구조 이해) |
| 3~4주차 | 로그인 + 공지사항 (가장 단순한 기능으로 전체 흐름 완성) |
| 5~6주차 | 벌점 부여(선생님) / 벌점 조회(학생) |
| 7~8주차 | 도시락 신청 / 도시락 관리 |
| 9~10주차 | 질문 접수·배정, 민원 신고·처리 |
| 11주차 | QR 순찰, 알림 발송 |
| 12주차 | 테스트, 버그 수정, 배포 (선생님 웹 Firebase Hosting 배포, Android APK 배포) |

주차별 날짜와 완료 기준은 `개발기획서.md` 7번을 따릅니다.

## 6. 내가 해야 할 일 (순서대로)

### 지금 당장
- [x] 기술 스택 결정 → Flutter + Firebase (2026-09-05)
- [x] 개발 도구 설치 (2026-09-05 완료, 7-1번 참고)
- [ ] 기획서의 기능 목록을 "꼭 필요한 것"과 "나중에 해도 되는 것"으로 나누기
- [x] 이 폴더를 Git 저장소로 만들기 (`git init`)

### 1단계: 기초
- [ ] `docs/바이브코딩-학습순서.md`의 0단계(터미널, Git 5개 명령, 폴더 지도, 실행 확인, AI에게 말하는 법)부터 순서대로 익히기
- [ ] 선택한 언어의 기본 문법 익히기 (변수, 함수, 조건문, 반복문)
- [ ] "Hello World" 앱을 PC와 모바일에서 각각 실행해 보기
- [ ] 화면(UI)과 데이터(서버)가 어떻게 연결되는지 개념 이해하기

### 2단계: 기능 개발 (5번 일정 참고)
- [ ] 기능 하나씩 만들고, 만들 때마다 실제 기기에서 실행해 확인
- [ ] 각 기능 완료 시 Git에 커밋

### 3단계: 배포
- [ ] Google Play 개발자 계정, Apple 개발자 계정 준비 (Apple은 유료, 심사 기간 있음)
- [ ] 개인정보 처리방침 작성 (미성년자 대상이므로 필수)
- [ ] 실제 학원에서 소규모 테스트

## 7. 기술 스택 (확정, 2026-09-05)

초보자가 3개월 안에 여러 플랫폼을 만들려면 **하나의 코드로 모든 플랫폼을 만드는 방식**이 거의 유일한 현실적 선택입니다.

| 영역 | 선택 | 이유 |
|------|------|------|
| 앱 프레임워크 | **Flutter** (Dart 언어) | 코드 하나로 Android, iOS, 웹을 모두 만든다. 배울 언어가 Dart 하나뿐이다. |
| 백엔드(서버) | **Firebase** | 서버를 직접 만들지 않는다. 로그인, 데이터베이스, 푸시 알림, 웹 호스팅을 제공한다. |
| 데이터베이스 | Firebase **Cloud Firestore** | 실시간 동기화. 선생님이 벌점을 주면 학생 화면에 바로 반영된다. |
| 로그인 | Firebase **Authentication** | 이메일/비밀번호. 학원에서 계정을 발급하는 방식. |
| 푸시 알림 | Firebase **Cloud Messaging (FCM)** | 공지, 도시락 마감, 질문 배정 알림 |
| 웹 배포 | Firebase **Hosting** | 선생님용 웹사이트를 무료로 배포 |
| 코드 관리 | **Git** + GitHub | 코드 변경 이력 저장, 백업 |
| 에디터 | **VS Code** + Flutter 확장 | 가볍고 Flutter 지원이 좋다 |
| Android 도구 | **Android Studio** | Android SDK, 에뮬레이터 |

- 프로젝트는 **하나의 Flutter 프로젝트**(`app/`)로 만들고, 로그인한 계정의 역할(학생/선생님)에 따라 다른 화면을 보여줍니다.
- 선생님용은 Windows 데스크톱 앱 대신 **웹사이트**로 결정했습니다. 설치가 필요 없어 배포가 쉽고, 순찰 중 태블릿/폰 브라우저에서도 쓸 수 있습니다.
- 검토했던 대안(React Native + Supabase)은 웹/데스크톱 지원과 학습 부담을 고려해 채택하지 않았습니다.
- 폴더 구조, 기능 명세, Firestore 데이터 설계는 `개발기획서.md`를 기준으로 합니다.

## 7-1. 개발 환경 (세팅 완료, 2026-09-05)

| 도구 | 위치 / 버전 |
|------|-------------|
| Flutter SDK | `C:\dev\flutter` (3.47.2 stable, Dart 3.13.2), 사용자 PATH에 등록 |
| Android SDK | `%LOCALAPPDATA%\Android\Sdk` (platform 36, build-tools 36.0.0, platform-tools), `ANDROID_HOME` 등록 |
| JDK | Android Studio 내장 JBR (`C:\Program Files\Android\Android Studio\jbr`), `flutter config --jdk-dir` 로 지정 |
| Android Studio | 설치됨. SDK와 에뮬레이터 용도이며 직접 열 일은 거의 없음 |
| 에뮬레이터 | `Pixel_8` (Android 36, Play Store 이미지) |
| VS Code | 설치됨 + Flutter/Dart 확장 |
| Node.js / Firebase CLI | v24 / firebase-tools 15.29 |
| Chrome | 설치됨 (웹 실행용) |
| Git | 2.55. 원격: https://github.com/KimHyungGyu0712/claude-test |

`flutter doctor` 결과: Visual Studio 항목만 X. Windows 데스크톱 앱은 만들지 않으므로 무시한다.
Python은 설치되어 있지 않다.
검증 완료 (2026-09-05): `flutter analyze`, `flutter test`, `flutter build web`, `flutter build apk --debug` 모두 성공.
**주의:** Gradle(Android 빌드 도구)은 동시에 두 개를 돌리면 캐시 잠금 오류가 난다. Android 빌드는 한 번에 하나만 실행한다.

**주의:** 터미널을 새로 열어야 PATH 변경이 적용된다. 이미 열려 있던 터미널에서는 `flutter` 명령이 안 보일 수 있다.

### 자주 쓰는 명령 (모두 `app/` 폴더 안에서)
```
flutter run -d chrome                 # 선생님 웹 화면을 Chrome에서 실행
flutter emulators --launch Pixel_8    # 에뮬레이터 켜기 (첫 실행은 1~2분)
flutter run                           # 켜진 에뮬레이터/연결된 폰에서 실행
flutter test                          # 테스트 실행
flutter analyze                       # 코드 오류 검사
flutter doctor                        # 환경 점검
```
실행 중 코드를 고치고 터미널에 `r`을 누르면 즉시 반영된다 (핫 리로드). `q`로 종료.

## 8. AI 에이전트 작업 규칙

- 개발자는 초보자이므로 **모든 설명은 전문 용어를 풀어서**, 왜 그렇게 하는지와 함께 설명합니다.
- 코드를 작성하면 **어떻게 실행해서 확인하는지**를 반드시 함께 알려줍니다.
- 한 번에 큰 기능을 만들지 않고, **작게 만들고 실행해서 확인**하는 방식으로 진행합니다.
- 새로운 도구나 라이브러리를 추가할 때는 이유를 먼저 설명하고 진행합니다.
- 기술 스택이 결정되면 이 파일의 7번 항목을 확정 내용으로 업데이트합니다.
- 결정 사항, 완료한 일, 막힌 문제는 이 파일에 계속 기록합니다.

## 9. 결정 기록

| 날짜 | 결정 내용 |
|------|-----------|
| 2026-09-05 | 프로젝트 시작. 개발 기간 3개월 (09-08 ~ 12-05). |
| 2026-09-05 | 기술 스택 Flutter + Firebase 확정. 선생님용은 PC 데스크톱 앱 대신 웹사이트, 학생용은 Android 우선·iOS 후순위로 결정. |
| 2026-09-05 | 프로젝트 폴더를 `C:\Users\khgg0\orca\claude-test` Git 저장소로 확정. 구체적인 개발은 orca ade에서 진행. |
| 2026-09-05 | 개발 환경 세팅 완료 (Flutter, Android SDK, 에뮬레이터 Pixel_8, VS Code, Firebase CLI). 한글 경로 폴더(`OneDrive\Desktop\학원어플`)는 Android 빌드 불가로 폐기. |
| 2026-09-05 | 선생님용 웹 UI 1차 완성 (`app/`). 가짜 데이터(메모리)로 화면 8개 동작. Firebase 연결은 3주차 계획대로 진행. 추가 라이브러리 없음. |

## 10. 현재 코드 상태와 검증 규칙

- 환경과 실행 명령은 **7-1번**을 따른다. 선생님 웹은 `app/` 에서 `flutter run -d chrome`, 로그인 화면의 "선생님으로 체험" 버튼으로 들어간다.
- **검증 규칙:** 코드를 바꾼 뒤에는 `flutter analyze`, `flutter test`가 통과하고, Chrome에서 실제로 화면을 눌러 본 뒤에만 "완료"로 본다. 배포 전에는 `flutter build web`도 확인한다.
- **현재 가짜인 것 (3주차에 Firebase로 교체):** 로그인은 아무 입력이나 통과. 데이터는 `lib/services/mock_data_service.dart`의 메모리에만 있어 새로고침하면 초기화된다.
- **학습 자료:** `docs/바이브코딩-학습순서.md`(전체 순서), `docs/0단계-따라하기.md`(터미널·Git·폴더·실행·AI 대화 실습), `docs/DB-학습로드맵.md`(Firestore → 보안 규칙 → SQL 순서. 3주차 Firebase 연결 때 1단계부터 시작)
